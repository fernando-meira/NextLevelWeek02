import React from "react";
import { View } from "react-native";
import styles from "../Landing/styles";

import { Container, Title } from "./styles";

const GiveClasses = () => {
  return (
    <Container>
      <Title>GiveClasses</Title>
    </Container>
  );
};

export default GiveClasses;
